import * as THREE from 'three'
import { AttackType } from '../utils/enums'

export default type => {
    switch (type) {
        case AttackType.ANIMAL:
        
        default:
            break;
    }
}